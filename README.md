# FuelRadarBot - Telegram-бот для краудсорсинга цен на бензин в Беларуси

MVP версия бота для отслеживания цен на топливо в Беларуси.

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Настройка окружения

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env .env
```

Откройте `.env` и укажите ваш токен Telegram-бота:

```
BOT_TOKEN=your_telegram_bot_token_here
YANDEX_MAPS_API_KEY=your_yandex_maps_api_key_here  # Опционально
```

**Как получить токен бота:**
1. Найдите [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям
4. Скопируйте полученный токен в файл `.env`

### 3. Запуск бота

```bash
python bot.py
```

При первом запуске автоматически создастся база данных `fuelradar.db` с начальными данными (15 АЗС в Минске).

## 📋 Команды бота

- `/start` - приветствие и краткая инструкция
- `/help` - подробная инструкция по использованию
- `/codes` - список всех сетей АЗС
- `/prices <город> <тип_топлива>` - поиск лучших цен
  - Пример: `/prices Минск 95`
- `/addprice` - интерактивное добавление новой цены

## 🏗️ Структура проекта

```
fuelradarbot/
├── bot.py                 # Главный файл запуска
├── config.py              # Конфигурация и константы
├── database/
│   ├── models.py          # Модели БД и инициализация
│   └── crud.py            # CRUD операции
├── handlers/
│   ├── start.py           # Команды /start, /help
│   ├── prices.py          # Команды /prices, /addprice
│   └── admin.py           # Админ-команды
├── keyboards/
│   └── inline_kb.py       # Inline клавиатуры
├── services/
│   ├── validation.py      # Валидация ввода
│   └── map_service.py     # Работа с картами
├── .env.example           # Пример env-файла
├── requirements.txt       # Зависимости
└── README.md              # Документация
```

## 🗄️ База данных

Используется SQLite с тремя основными таблицами:

- **users** - пользователи бота (user_id, username, rating)
- **azs** - заправки (network, address, city, lat, lon)
- **prices** - цены на топливо (azs_id, fuel_type, price, user_id, timestamp)

## 🔧 Технологии

- **Python 3.8+**
- **aiogram 3.x** - асинхронная библиотека для Telegram Bot API
- **aiosqlite** - асинхронная работа с SQLite
- **python-dotenv** - загрузка переменных окружения

## 📝 Особенности MVP

- ✅ Интерактивное добавление цен через кнопки
- ✅ Поиск лучших цен по городу и типу топлива
- ✅ Валидация всех входных данных
- ✅ Автоматическая инициализация БД с тестовыми данными
- ✅ Логирование всех операций
- ✅ Обработка ошибок

## 🚧 Будущие улучшения

- [ ] Поддержка всех городов Беларуси
- [ ] Статистика и графики цен
- [ ] Уведомления об изменении цен
- [ ] Рейтинг пользователей
- [ ] Модерация цен
- [ ] Интеграция с картами
- [ ] Экспорт данных

## 📞 Поддержка

При возникновении проблем проверьте:
1. Правильность токена в `.env`
2. Установлены ли все зависимости
3. Логи в консоли для диагностики ошибок

## 📄 Лицензия

Проект создан для образовательных целей.

